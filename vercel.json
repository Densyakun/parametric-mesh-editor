{
  "version": 2,
  "buildCommand": "pnpm build",
  "outputDirectory": "packages/app/dist",
  "installCommand": "pnpm install",
  "framework": "vite",

  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
